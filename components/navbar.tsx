import React from "react";

const NavBar = () => {
  return (
    // TODO: Implement a navbar for user side maybe? not 100% if this is the route we want to take
    <div>navbar</div>
  );
};

export default NavBar;
